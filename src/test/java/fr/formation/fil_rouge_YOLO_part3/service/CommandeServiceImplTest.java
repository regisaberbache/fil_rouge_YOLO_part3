package fr.formation.fil_rouge_YOLO_part3.service;

class CommandeServiceImplTest {
	// le satut peut etre en attente, en cours ou termine
	//si le statut est en attente, on peut modifier la commande
	//si le statut est en cours, on ne peut pas modifier la commande
	//si le statut est termine, on ne peut pas modifier la commande
	//si le statut est en attente, on peut supprimer la commande
	//si le statut est en cours, on ne peut pas supprimer la commande
	//si le statut est termine, on ne peut pas supprimer la commande
	//si le statut est en attente, on peut ajouter une ligne de commande
	//si le statut est en cours, on ne peut pas ajouter une ligne de commande
	//si le statut est termine, on ne peut pas ajouter une ligne de commande
	//si le statut est en attente, on peut supprimer une ligne de commande
	//si le statut est en cours, on ne peut pas supprimer une ligne de commande
	//si le statut est termine, on ne peut pas supprimer une ligne de commande
	





}
